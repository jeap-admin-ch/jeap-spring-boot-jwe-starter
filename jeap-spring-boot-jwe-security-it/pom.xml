<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.admin.bit.jeap</groupId>
        <artifactId>jeap-spring-boot-jwe-starter-parent</artifactId>
        <version>1.9.0-SNAPSHOT</version>
    </parent>

    <artifactId>jeap-spring-boot-jwe-security-it</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>
    <description>Integration tests verifying that the JWE starter coexists with jeap-spring-boot-security-starter
        (Bearer-token authentication and transparent JWE encryption on the same servlet chain). Test-only module so
        the jeap-security dependency stays out of the published starter artifacts.
    </description>
    <packaging>jar</packaging>

    <properties>
        <jeap-starter.version>23.9.0</jeap-starter.version>
    </properties>

    <dependencies>
        <!-- System under test: the JWE starter, used exactly as a consuming service would. -->
        <dependency>
            <groupId>ch.admin.bit.jeap</groupId>
            <artifactId>jeap-spring-boot-jwe-starter</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- The other half of the coexistence test: the jeap-security resource-server starter. Scoped to
             test so it never leaks onto the published starter's classpath. -->
        <dependency>
            <groupId>ch.admin.bit.jeap</groupId>
            <artifactId>jeap-spring-boot-security-starter</artifactId>
            <version>${jeap-starter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.admin.bit.jeap</groupId>
            <artifactId>jeap-spring-boot-security-starter-test</artifactId>
            <version>${jeap-starter.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Servlet web stack hosting the filter, the controllers and the JWKS endpoints. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Static RSA test keys, so the IT runs without Vault. -->
        <dependency>
            <groupId>ch.admin.bit.jeap</groupId>
            <artifactId>jeap-spring-boot-jwe-test</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <id>empty-javadoc-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <classifier>javadoc</classifier>
                            <classesDirectory>${project.build.directory}/empty-javadoc</classesDirectory>
                            <forceCreation>true</forceCreation>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
